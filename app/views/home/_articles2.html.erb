  
      <% categories = taxonomies.second %>
        <% cache [I18n.locale, categories, @taxon.children] do %>
          <%= render(TaxonsTreeComponentArticles2.new(
            root_taxon: categories.root,
            current_taxon: @taxon.children,
            max_level: 1,
            base_class: 'categories')) if categories 
            %>
        <% end %>    
